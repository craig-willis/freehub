%h1 Welcome, #{link_to @person.first_name, person_path(:id => @person)}
%p
  Welcome to the Bike Project of Urbana-Champaign.
  -if @person.membership
    Message to member.
    #{link_to 'Click here', membershipcard_path(:person_id => @person, :format => :pdf)} to print your membership card.
  -else
    As a public patron, you are allowed two free visits.
    #{link_to 'Click here', new_service_path(:person_id => @person)} to start your membership.
%p
  #{link_to 'Return to', @organization} sign-in page
